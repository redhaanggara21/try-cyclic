import { schema, categoryInput, categoryMutation, categoryType } from "./category.typedef";

const typedef = [
    categoryInput,
    categoryMutation,
    categoryType,
    schema
];

export default typedef;